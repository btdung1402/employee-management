spring.application.name=employeemanagement

# spring.datasource.url=${DB_URL_EM}
# spring.datasource.username=${DB_USERNAME}
# spring.datasource.password=${DB_PASSWORD}
spring.datasource.url=jdbc:mysql://localhost:3306/employeemanagement
spring.datasource.username=root
spring.datasource.password=thanhtung03
spring.datasource.hikari.maximum-pool-size=1

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

logging.level.org.springframework.security = DEBUG

# jwt.expiration= ${EXPIRATION}
# jwt.secretKey= ${SECRET_KEY}
jwt.expiration=${EXPIRATION:60000}   
jwt.secretKey=${SECRET_KEY:YKX7J5M9HrMFLsCgU6NpGtz8oq51nQGvkLu6F9RWOW4=}

spring.mail.host=smtp.gmail.com
spring.mail.port=587
# spring.mail.username=${MAIL_ACCOUNT}
# spring.mail.password=${MAIL_PASSWORD}
# spring.mail.properties.mail.smtp.auth=true
# spring.mail.properties.mail.smtp.starttls.enable=true

# spring.data.redis.host=${REDIS_HOST}
# spring.data.redis.port=${REDIS_PORT}
# spring.data.redis.password=${REDIS_PASSWORD}